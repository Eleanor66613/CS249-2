bnames<-read.csv('baby-names.csv)
bnames <- read.csv("baby-names.csv", stringsAsFactors = FALSE)
print(dim(bnames)
)
print(head(bnames)
)
names_ignore_sex<-ddply(bnames, c("name"))
    
library(bitops)
library(RCurl)
library(plyr)
library(ggplot2)
names_ignore_sex<-ddply(bnames, c("name"))
print(head(names_ignore_sex))
print(names_ignore_sex)
names_ignore_sex<-ddply(bnames, c("name","year"),sump)
bnames$sex <- factor(bnames$sex, levels = c("boy", "girl"))
sump <- colwise(sum, "percent")
names_ignore_sex<-ddply(bnames, c("name","year"),sump)
names_ignore_sex<-ddply(bnames, c("name","year"))
names_ignore_sex<-ddply(bnames, c("name"))

names_ignore_sex<-ddply(names_ignore_sex,sump)
names_ignore_sex<-ddply(names_ignore_sex,.(name,year),sump)
letter <- function(string, pos) {
  if (pos > 0) {
    substr(string, pos, pos)
  } else {
    nc <- nchar(string)
    substr(string, nc + pos + 1, nc + pos + 1)
  }
}
names_first<-subset(names_ignore_sex, letter(name, 1)=="A")
print(names_first)
names_ignore_sex_first<-ddply(names_first, .(name, year), sump)
print(names_ignore_sex_first)
ls
ls()
nrow(names_first)-nrow(names_ignore_sex_first)
names_A<-names_first
ls()
times<-ddply(bnames,
      c("name"),
      summarise,
         boys = sum(percent[sex == "boy"]),
         boys_n = sum(sex == "boy"),
         girls = sum(percent[sex == "girl"]),
         girls_n = sum(sex == "girl"),
         .progress = "text"
)
print(head(times))
times<-subset(times, boys_n+girls_n>=15)
select_names<-times$name
candidate<-subset(bnames, name %in% select_names)
print(head(candidate))
nrow(bnames)-nrow(candidate)
nrow(bnames)
names_ignore_sex_A<-subset(candidate, letter(name, 1)=="A")
rnow(names_ignore_sex_A)-rnow(names_ignore_sex_first)
nrow(names_ignore_sex_A)-nrow(names_ignore_sex_first)
names_ignore_sex_AA<-ddply(names_ignore_sex_A, .(name, year), sump)
nrow(names_ignore_sex_AA)-nrow(names_ignore_sex_A)
print(names_ignore_sex_AA)
ls()
source("rankCode.R")
source("rankCode.R")
source("rankCode.R")
source("rankCode.R")
checker(names_ignroe_sex_AA)
ls()
checker(names_ignore_sex_AA)
print(head(names_ignore_sex_AA))
result<-checker(names_ignore_sex_AA)
source("rankCode.R")
result<-checker(names_ignore_sex_AA)
source("rankCode.R")
result<-checker(names_ignore_sex_AA)
print(result)
names_ignore_sex<-ddply(bnames, c(name, year), sump)
names_ignore_sex<-ddply(bnames, .(name, year))
names_ignore_sex<-ddply(bnames, .(name, year), sump)
names_ignore_sex<-ddply(bnames, .(name, year), sump, .progress="text")
print(head(names_ignore_sex))
print(tail(names_ignore_sex))
freq_names<-ddply(names_ignore_sex, c("name"),summarise, count=sum(length(year)))
print(head(freq_names))
freq_names<-subset(freq_names, count>=15)
print(head(freq_names))
candidate<-subset(names_ignore_sex, name %in% freq_names)
print(head(candidate))
print(names_ignore_sex)
print(head(names_ignore_sex))
candidate<-subset(names_ignore_sex, name %in% freq_names$name)
print(head(candidate))
source("rankCode.R")
result<-checker(candidate)
print(result)
print(subset(candidate, name=="Andrew"))
source("rankCode.R")
result<-checker(candidate)
print(result)
name_ranked <- ddply(
    name_percentage, .(name), mutate,
    rank_percent = rank(-percent),
    rank_year = rank(-year)
    rank_percent = rank(-percent),
name_ranked<-ddply(candidatem .(name), mutate, rank_percent=rank(percent), rank_year=rank(year))
name_ranked<-ddply(candidate .(name), mutate, rank_percent=rank(percent), rank_year=rank(year))
name_ranked<-ddply(candidate .(name), mutate, rank_percent=rank(percent), rank_year=rank(year))
print(head(candidate))
name_ranked<-ddply(candidate .(name), mutate, rank_percent=rank(-percent), rank_year=rank(-year))
name_ranked<-ddply(candidate, .(name), mutate, rank_percent=rank(-percent), rank_year=rank(-year))
print(name_ranked)
print(head(name_ranked))
print(head(candidate))
name_ranked<-ddply(candidate, .(name), mutate, rank_percent=rank(percent), rank_year=rank(year))
print(head(name_ranked))
names_no_trend<-unique(subset(name_ranked,rank_percent==rank_year)$name)
print(name_no_trend)
print(names_no_trend)
names_no_trend<-unique(subset(name_ranked,rank_percent!=rank_year)$name)
print(subset(bnames, !name %in% names_no_trend))
up_trending_name <- unique(subset(name_ranked, !(name %in% no_trending_name))$name)
up_trending_name <- unique(subset(name_ranked, !(name %in% name_no_trend))$name)
up_trending_name <- unique(subset(name_ranked, !(name %in% names_no_trend))$name)
print(up_trending_name)
print(freq_names)
freq_name_candidate<-ddply(names_ignore_sex, c("name"), summarise, cout=sum(length(year))
freq_name_candidate<-ddply(names_ignore_sex, c("name"), summarise, cout=sum(length(year))
freq_name_candidate<-ddply(names_ignore_sex, c("name"), summarise, count=sum(length(year))
freq_name_candidate<-ddply(names_ignore_sex, c("name"), summarise, count=sum(length(year))
print(head(names_ignore_sex))
freq_name_candidate<-ddply(names_ignore_sex, .(name), summarize, count=sum(length(year)))
freq_names<-subset(freq_name_candidate, count>=15)
print(head(freq_names))
candidate<-subset(names_ignore_sex, names %in% freq_names)  
print(head(names_ignore_sex))
candidate<-subset(names_ignroe_sex, name %in% freq_names)
candidate<-subset(names_ignore_sex, name %in% freq_names)
print(head(candidate))
print（head(freq_names)）
print（head(candidate)）
print(head(freq_names))
candidate<-subset(names_ignore_sex, name %in% freq_names))
candidate<-subset(names_ignore_sex, name %in% freq_names)
print(head(candidate))
freq_name_candidate<-ddply(names_ignore_sex, c("name"), summarise, cout=sum(length(year)))
freq_names<-subset(freq_name_candidate, count>=15)
freq_names<-subset(freq_name_candidate, cout>=15)
candidate<-subset(names_ignore_sex, name %in% freq_names) 
print(head(candidate))
print(head(freq_names))
candidate<-subset(names_ignore_sex, name %in% freq_names$name) 
print(head(candidate))
result<-checker(candidate)
print(result)
